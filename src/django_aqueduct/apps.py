"""Django app configuration for django-aqueduct."""

from django.apps import AppConfig


class AqueductConfig(AppConfig):
    """AppConfig for django-aqueduct."""

    name = "django_aqueduct"
    verbose_name = "Django Aqueduct"
